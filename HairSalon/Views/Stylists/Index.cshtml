@using HairSalon.Models;
@{
  Layout = "_Layout";
}

<h1> All Stylists </h1>
<h3> Current Available Stylists: @Model.Count </h3>
@if(@Model.Count == 0)
{
  <h3> No Stylists Available</h3>
}

<ul>
  @foreach(Stylist stylist in Model)
  {
    <li></li>
  }
</ul>

<p>@Html.ActionLink("Add a new Stylist", "Create")</p>
<p>@Html.ActionLink("Go back to Homepage", "Index", "Home")</p>